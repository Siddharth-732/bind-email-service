PORT=4000
REDIS_URL=
MONGODB_URI=

# Brevo Transactional Email API (HTTPS, not SMTP - many free hosts block outbound SMTP ports)
# Brevo free tier: 300 emails/day, no credit card, no domain required.
BREVO_API_KEY=      # Brevo dashboard -> SMTP & API -> API Keys tab -> Generate a new API key
EMAIL_FROM_ADDRESS= # must be a provider-verified sender address
